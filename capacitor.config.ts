import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.browserApp',
  appName: 'in-app-browser',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
